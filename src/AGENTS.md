# Frontend agent notes

This tree is owned by the **frontend** subagent (`.cursor/agents/frontend.md`).

- Compose thin pages from named `ui` / feature components
- Talk to the backend only via `shared/api` `invoke`
- If a command/DTO is missing, document the contract — do not edit `src-tauri/`
