name: Windows signing checks

on:
  workflow_call:

permissions:
  contents: read

jobs:
  signing:
    runs-on: windows-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Check signing and verification with disposable certificates
        shell: pwsh
        run: ./scripts/test-windows-signing.ps1
