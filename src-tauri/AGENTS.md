# Backend agent notes

This tree is owned by the **backend** subagent (`.cursor/agents/backend.md`).

- commands → service → repository → SQLite
- Trusted validation lives here; return camelCase DTOs for the UI
- Do not edit React under `src/` — return the frontend contract instead
